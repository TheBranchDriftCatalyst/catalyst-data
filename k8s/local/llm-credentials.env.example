# Copy to llm-credentials.env (gitignored) and fill with the LiteLLM
# master key (same value as catalyst-llm's `master-key`).
# Mirrors ExternalSecret `llm-credentials`
# (k8s/talos00/externalsecret-llm.yaml → 1Password `litellm.master-key`).
LLM_API_KEY=sk-litellm-REPLACE_ME
