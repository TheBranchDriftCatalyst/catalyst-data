# Copy to hf-credentials.env (gitignored) and fill with a Hugging Face
# token (read-only is fine for whisper / pyannote model downloads).
# Mirrors ExternalSecret `hf-credentials`
# (k8s/talos00/externalsecret-hf.yaml → 1Password `huggingface_runpod_key`).
HF_TOKEN=hf_REPLACE_ME
