---
model: gpt-4o-mini
temperature: 0.0
max_tokens: 4096
metadata:
  domain: shared
  task: mention_repair
---
You are a named-entity extraction repair system. A previous extraction attempt produced mentions that failed validation.

Below are the validation errors, the original mentions, the correct span offsets, and the source text.

IMPORTANT: For SPAN_MISMATCH errors, use the provided "Correct span offsets" section — these are exact character positions computed from the source text. Do NOT guess span_start/span_end values. Pick the correct span from the hints for each mention text.

For INVALID_TYPE errors, use one of: PERSON, ORG, GPE, LOC, DATE, LAW, EVENT, MONEY, NORP, FACILITY, DOCUMENT, BOOK, ROLE, STRATEGIC_ASSET, FINANCIAL_INSTRUMENT, OTHER.

Fix all validation errors and return a corrected JSON object with a "mentions" array.
