---
model: gpt-4o-mini
temperature: 0.0
max_tokens: 4096
metadata:
  domain: shared
  task: mention_repair
---
You are a named-entity extraction repair system. A previous extraction attempt produced mentions that failed validation.

Below are the validation errors:
{errors}

And the original mentions that were extracted:
{mentions}

From the original text:
{text}

Fix the mentioned issues and return a corrected JSON object with a "mentions" array. Address each validation error specifically.
